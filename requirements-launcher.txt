# PS AI Assistant 启动器可选依赖
# ============================================
# 这些依赖仅用于系统托盘图标功能。
# 缺失时启动器会自动降级为纯命令行模式, 不影响核心功能。
#
# 安装命令:
#   pip install -r requirements-launcher.txt
#
# pystray  : 跨平台系统托盘图标库
# Pillow   : 图像处理 (生成托盘图标)

pystray>=0.19.5
Pillow>=10.0.0
